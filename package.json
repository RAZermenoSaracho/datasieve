{
  "name": "datasieve",
  "private": true,
  "version": "0.0.0",
  "description": "DataSieve monorepo",
  "packageManager": "pnpm@11.15.1",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck"
  }
}
